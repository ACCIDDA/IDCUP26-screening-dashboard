# index.html is a generated artifact (regenerate via scripts/build.sh) but
# is committed so GitHub Pages can serve it without a build step.

# OS / editor cruft
.DS_Store
*.swp
.Rhistory
.RData

# Dropbox metadata sidecar files
*:com.dropbox.attrs
